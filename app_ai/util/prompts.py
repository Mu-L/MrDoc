# 索引生成Prompt
def build_index_prompt(markdown_content: str) -> str:
    return f"""
你是一个“知识库结构化引擎”。

请对以下 Markdown 文档进行分析，并输出结构化 JSON。

输出格式如下：

{{
  "summary": "文档摘要",
  "sections": [
    {{
      "title": "章节标题",
      "summary": "核心要点总结（必须具体）",
      "keywords": ["用户搜索词"],
      "tags": ["分类标签"],
      "faqs": [
        {{
          "q": "用户真实问题",
          "a": "基于原文的回答",
          "ref": "章节标题"
        }}
      ]
    }}
  ]
}}

【核心强化要求】

一、summary（非常重要）
- 必须是“可回答问题的内容”，而不是概述
- 必须包含具体信息（功能 / 限制 / 数字 / 条件）
- 禁止使用“本文介绍…”、“本节说明…”
- 示例：
  ❌ 介绍了权限管理
  ✅ 支持公开、私密和指定用户三种权限；指定用户最多支持100人

---

二、section.summary（最关键）
- 每一句必须包含“有效信息”（功能 / 限制 / 数据）
- 优先提取：
  - 怎么做
  - 有什么限制
  - 支持什么
- 如果一句话没有信息量 → 删除
- 使用短句 + 分号分隔

---

三、keywords（用于搜索）
- 必须是用户可能搜索的词
- 优先使用：
  - 功能名（权限配置）
  - 动作词（如何设置权限）
  - 场景词（团队协作权限）
- 禁止：
  ❌ ["介绍", "功能"]
  ✅ ["权限设置", "用户权限限制"]

---

四、FAQ（非常关键）
- 必须模拟真实用户问题（口语化）
- 优先生成：
  - 如何类（怎么设置权限？）
  - 限制类（最多支持多少人？）
  - 对比类（公开和私密有什么区别？）
- 回答必须：
  - 简洁
  - 基于原文
  - 包含具体信息

---

五、严格规则
- 不允许编造内容
- 所有信息必须来自原文
- 输出必须是合法 JSON
- 不要输出解释

---

【文档内容】
{markdown_content}
"""

def build_section_prompt(markdown_content: str) -> str:
    return f"""
你是一个“高级知识库结构化引擎”，你的任务是将文档转化为高质量、可用于AI问答的结构化数据。

========================
【核心目标】
========================
- 输出“语义完整”的知识块（section）
- 每个section必须能独立回答问题
- 避免信息丢失、避免语义断裂

========================
【分段规则（非常重要）】
========================

1️⃣ 按“语义”分段，而不是按长度机械切分

2️⃣ 每个section必须：
- 表达一个完整主题（如：一个功能、一组步骤、一个概念）
- 可以独立回答用户问题

3️⃣ 长度控制：
- 理想长度：200 ~ 800字
- >1000字：必须拆分
- <100字：必须合并到相邻主题（除非是明确概念定义）

4️⃣ 强制保持语义完整：
以下内容绝对不能拆开：
- 步骤（1、2、3）
- 配置流程
- 成对说明（优缺点、对比）

5️⃣ 优先按以下结构切分：
- 标题（# ## ###）
- 列表（步骤/要点）
- 功能模块

========================
【summary要求（非常关键）】
========================

每个section的summary必须：

- 不是“概括”，而是“可用于回答问题”
- 必须包含：
  - 关键对象（做什么）
  - 关键方式（怎么做）
  - 关键结果（效果/结论）

❌ 错误示例：
"介绍了认证方式"

✅ 正确示例：
"系统支持OAuth2、Token和API Key三种认证方式，适用于不同安全需求场景"

========================
【FAQ生成规则】
========================

每个section必须生成2~4个FAQ：

要求：
- 问题必须是“用户真实会问的”
- 使用口语化表达（如：怎么、如何、为什么）
- 避免书面语

示例：
❌ “认证方式有哪些？”
✅ “系统支持哪些登录方式？”
✅ “怎么配置OAuth2登录？”

答案要求：
- 必须基于原文
- 可以略微重写，但不能编造

========================
【keywords规则】
========================

- 提取用户可能搜索的词
- 包含：
  - 同义词（登录=认证）
  - 技术词（OAuth2）
  - 场景词（权限、安全）

数量：3~8个

========================
【输出格式（必须严格JSON）】
========================

{{
  "summary": "整篇文档摘要（必须具体，包含关键能力）",
  "sections": [
    {{
      "title": "清晰的主题标题",
      "summary": "可直接用于回答问题的总结",
      "content": "该section对应的完整原文（必要时可轻微整理，但不能删减信息）",
      "keywords": ["搜索词"],
      "tags": ["分类标签"],
      "faqs": [
        {{
          "q": "用户真实问题",
          "a": "基于原文的回答"
        }}
      ]
    }}
  ]
}}

========================
【硬性规则（必须遵守）】
========================

- ❗ 不允许编造内容
- ❗ 不允许丢失重要信息
- ❗ 必须输出合法JSON
- ❗ section数量必须合理（通常3~10个）
- ❗ content必须保留原文核心信息（用于RAG）

========================
【文档内容】
========================
{markdown_content}
"""

# RAG问答Prompt
def build_answer_prompt(question, sections):
    # context = "\n\n".join([
    #     f"[{i+1}]【{sec['doc_title']}】\n{sec['content']}"
    #     for i, sec in enumerate(sections)
    # ])
    context = "\n\n".join([
        f"[{i + 1}]【{sec['section_title']}】\n"
        f"路径：{sec['title_path']}\n"
        f"来源：{sec['doc_title']}\n"
        f"{sec['content']}"
        for i, sec in enumerate(sections)
    ])

    return f"""你是一个知识库问答助手，请根据【参考内容】回答【问题】。

【参考内容】
{context}

【问题】
{question}

【作答要求】
1. 只根据【参考内容】作答，回答中的每个关键信息都必须在参考内容中有依据，并标注来源编号，如 [1]、[2]；参考内容可能来自不同章节，信息互补或重复时可综合多块内容回答；
2. 【冲突识别】如果多个参考片段描述同一实体、职位、事件或事实，但存在不同的人名、数字、时间、届次或结论，必须识别这种冲突；仅措辞不同、精度不同或互为补充的信息不视为冲突；
3. 【冲突消歧】遇到冲突时，应优先依据问题中的明确限定条件进行判断，例如时间、届次、事件名称、会议名称等；不得仅因为某个事实看起来更新、某个文档排名更高或某个说法出现次数更多，就认定其一定正确；
4. 【无法唯一确定】如果现有证据仍无法唯一确定答案，应明确说明存在冲突，并分别给出相关事实及其来源编号，而不是自行猜测；
5. 【多子问题】对于包含多个子问题的问题，应逐项确认每个子问题都有对应证据后再作答；若任一部分缺少对应证据，明确说明该部分在参考内容中未找到，不得推测补充；
6. 回答前请自查以下事项（内部检查即可，无需在答案中输出检查过程）：是否回答了问题中的每一个子问题？是否遗漏了参考内容中的关键事实？是否存在相互冲突的证据？如果存在冲突，是否已经根据问题限定条件完成消歧？最终答案中的每个关键结论是否都有对应证据？
7. 仅当参考内容与问题完全无关时，回答"未在文档中找到相关内容"；
8. 回答要清晰、简洁。"""